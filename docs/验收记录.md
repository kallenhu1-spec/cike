# 2026-09-23 · 参考阅读与规划基线

- 方案定案但未实现：生产层为 Skill + 多模态/编码大模型 + Seedream，运行层为本地角色部件 + 可复用 SVG/分层动作时间线；唯一输入为一张用户授权照片和一种风格感觉；输出契约为角色母版、部件、至少 6 个动作示例、预览和回退。
- 待验证：两张不同照片是否能复用同一套动作模板；角色身份、手/道具接触、动作连续性、末帧、小尺寸辨识和资源成本。未把参考项目的效果当成已验证。
- 新增优先候选：Rive 作为可交互动画运行层，输入为可绑定 SVG 部件，输出为带 State Machine/Listeners/Bones/IK 的 `.riv` 角色包；当前 SVG/分层时间线作为兜底。待验证导出授权/费用、Electron 集成、两个角色共用一套状态机、交互输入、末帧和资源占用。
- 用户已确认的能力方向：角色与场景小物必须能发生动作关系；首批验收为杯子喝水、杯子放回、物品放下、打开/合上本子。待验证手/道具接触、遮挡层级、抓取/释放、动作中断、末帧与小尺寸辨识。

- 本轮新增已验证阅读：Chrome Web Store 插件页和插件隐私政策可直接打开；4 条小红书中 3 条读取到正文/评论，1 条只读取到标题/标签/互动信息。未登录、未扫码、未安装插件、未执行作者 Skill。
- 本轮未验证：插件真实权限、模型请求、记忆质量、资源占用；小红书配图逐项视觉、视频完整播放/逐帧和音频；作者宣称的“一行代码/2分钟/任何形象”。
- 新增规划检查：在动作原型中记录 SVG/分层资产与 GIF/短视频/当前关键帧的视觉和资源成本；在离线对话原型中记录本地记忆开关、清除、发送范围和默认断网行为。

- 已验证：使用已登录浏览器实际打开并读取 4 条新增小红书笔记的文本、正文、标签、页面互动数和部分评论；第 5 条链接返回 404。来源和逐条读取范围见 `docs/production/收藏逐篇阅读记录.md`。
- 未验证：本轮未完成新增笔记的图片逐项视觉审阅、视频完整播放/逐帧检查或音频/字幕读取；未执行作者提供的 Skill、桌宠生成指令或本地模型方案。
- 当前产品基线：0.9.0 已有 Seedream 形象候选和动作配图代码，但真实 API、费用、生成效果和自然动画尚未验证；离线小模型没有实现。
- 下一步边界：先做照片主体识别到角色母版、动作资产生产和受限离线对话的可操作原型；用户确认后再实现。动作原型需并排验证 GIF、短视频、分层/骨骼和现有关键帧的手部与道具接触。

# 0.8.1 小动作画廊 · 2026-09-22

- 48项既有单测通过；本轮主要为配置中心呈现与交互变化，不新增模拟实现的单元测试。
- 最终0.8.1固定包scripts/gallery-smoke.cjs通过：136张真实画廊卡片，初始不展示编辑表单，点卡片才打开弹窗，更多设置默认折叠；预览不改内置覆盖；取消换图不保存、确认后仅图片变化，原文字/时段/语气/动作完整保留；卡片启停、搜索空态、新建、760px无横向溢出、重启持久化、零页面错误。
- 图片读取使用本地测试图片，系统文件选择器用测试替身，图像处理/窗口/持久化均为实际最终应用；没有读取或更改用户私人录音。
- 已检查最终截图：artifacts/0.8.1/小动作画廊.png、简洁编辑.png、画廊窄窗口.png。静态缩略图沿用真实动作与已保存外观，不是新生成概念图。
- 声音与衣橱核心未变，本轮未重跑其专项、未做真实AI生成/麦克风/跨机器测试。Mac arm64未签名公证。待用户评审；未提交、推送或公开发布。

---

# 0.8.0 由你定制 · 2026-09-22

- 48项单测通过，新增内置覆盖/图片持久化、时段回退、备份校验与不可信输入拒绝。
- 最终固定包scripts/customize-smoke.cjs通过：空资料/无引擎启动、应用版本、六项真实原生菜单构建与暂停回调、小动作创建/保存/时段/预览/台词配音跳转、图片先预览再应用与2:1比例保持、逐项配图与停用、重启保留、配置恢复及恢复前副本、760px布局无横向溢出、零页面错误。文件选择和恢复确认使用对话框替身，图片处理、存储与真实窗口为最终包实际实现；不冒充人工点击系统文件选择器验收。
- 最终0.8.0包配音回归通过：合成测试信号编解码、试听/启用、静音、击掌、重启、删除、窄屏、模拟录音与麦克风拒绝、IPC隔离。本轮未运行真实AI推理或真实麦克风采集。
- 最终0.8.0包衣橱回归通过：五款衣服、试穿隔离、保存/重启/恢复、皮肤与五官保留、60组外观和35种动作解码、击掌、760px布局。未更换已存在角色素材。
- 实际截图在artifacts/0.8.0；小动作截图来自测试文字，形象导入使用故意制成2:1的本地测试图验证等比，不是新美术设计。无私人录音或原始用户参考照片写入交付包。
- 待用户评审；未提交、推送、公开发布、签名公证或其他机器安装验收。AI引擎安装与任意角色自动动作生成不属于本轮新增能力。手机配置未实现且不属于本轮范围。

---

# 0.7.2 声音随你 · 2026-09-22

晚间分享准备核查：已查看用户提供的菜单截图并读取配置、菜单、图片导入、外观、声音存储及引擎查找源码；确认本机Application Support/cike-companion目录存在。未读取或复制私人音频。本轮未启动应用、未重跑测试、未修改应用；以下测试属于此前交付证据。菜单收简、空档案无引擎启动、其他机器安装仍待验证；本轮不声明完整流程视觉审查完成。

- 45/45单测通过。最终包scripts/voice-072.cjs通过：真实右键菜单构建与开关回调、跨窗口同步、逐句静音及恢复、重载VoiceStore持久化、原声保留、覆盖数、五句队列跳过不配音及仅试听句、无页面错误。队列推理使用测试替身，没有生成真实AI声音。
- 最终包原有test:voice通过：音频播放、卡片收起停止、启用/待审隔离、总静音、击掌、应用重启持久化、删除、620px窄屏、模拟设备录制与拒绝路径。使用隔离档案，不采集真实麦克风。
- 实际读取私人资料的元数据以诊断覆盖情况，未复制、上传或改写音频；未试听孩子声音、未验证声线或新增真实AI推理。
- 截图位于artifacts/0.7.2。旧回归脚本写死0.7.0截图路径，本轮运行时覆盖了两张历史图；已将新版图归入0.7.2，并从固定0.7.0包重新截图恢复对应版本证据。回归脚本已支持独立输出目录，避免再次发生。历史截图为本次复拍，非原拍摄文件。
- 包内源码、package版本与恢复快照对应核查见manifest.json；未提交、推送或发布。Mac arm64未签名公证，待用户体验确认。

---

# 0.7.1 击掌与晚安 · 2026-09-22

- 45/45既有单测通过；最终包 scripts/fixes-071.cjs 验证普通卡入口可见、击掌后按钮隐藏、3秒前静音裁剪、原始WAV保留、5秒声音播放结束后收起、22/05/06时边界、睡觉不轮换及无页面错误。
- 测试用独立临时资料与合成信号，未读取、上传或改写孩子原始录音。时钟为模拟边界，实际图来自最终应用窗口。用户自定义图片不会改为闭眼。
- 首尾静音按10ms窗口低能量检测，保留120ms前边界和180ms后边界；不删除句内停顿。已有录音也生效；轻声是否误裁需要家长试听。没有固定删除几秒，没有改变AI参考原件。
- macOS arm64本地包未签名公证；未重验真实AI生成、麦克风、长期调度、Windows。Git无提交，未推送发布；版本对应和快照见版本索引。

---

# wardrobe-v2-20260922 · 服装面积、五官上移与调色

- 45/45 单测通过；新增向上位置与旧位置范围兼容检查。穿衣时五官上移22个绘图单位，服装围绕固定下摆向上扩展，花纹避让跟随脸部位置。
- 最终 `dist/wardrobe-v2-20260922/mac-arm64/此刻.app` 通过 `scripts/wardrobe-smoke.cjs`：5个穿着选项、常用色点击、自选色、五官-1、皮肤保留、应用/重启/恢复、穿衣击掌；60种形象与35种动作真实解码，760px无横向溢出。
- 本轮截图独立保存在 `artifacts/wardrobe-v2-20260922/`，已查看实际界面和款式截图，确认领口与脸分开、衣服明显扩大；最终用户视觉评审待完成。
- 保留0.7.0配音源码，独立应用目录已配置同一本机voice-runtime链接。本轮未重新运行声音AI生成或全套历史UI测试；不新增其验收声明。
- 未提交、推送、发布。源码恢复快照同目录 `source.tar.gz`；旧版入口和截图保留。

---

# 0.7.0 · 小小配音室（2026-09-22）

- 基线为 0.6.1，保留同工作区同期小衣橱改动；本轮没有重画角色。用户将最初笔误“小纸盆”明确纠正为“小纸团”，界面与录音示例已全部改正。
- 44/44 单测通过（含同期衣橱 3 项），新增声音文件校验、精确台词匹配、待审不能自动播放、持久化/删除/设置、无引擎报错、损坏档案保留、生成临时文件清理。
- `npm run test:voice` 开发态通过；指定 `CIKE_EXECUTABLE` 为最终 `dist/0.7.0/mac-arm64/此刻.app/Contents/MacOS/此刻` 同样通过。真实窗口检查：入口、同意提示、WAV 编解码、待试听/启用、精确正文配音（昵称不影响匹配）、卡片收起停止、静音、击掌、重启、删除、清除原生对话框保留/确认分支、620px 窄窗口、麦克风拒绝、合成音源录制/停止与轨道释放、非配音窗口不能读取样本。页面错误为零。
- `CIKE_TEST_AI=1` 对最终包的真实 AI 验收通过：系统合成参考声音 → 样本保存 → 主进程启动本机模型 → 新台词 WAV → 待试听状态；另一次生成取消后临时目录无残留。使用 macOS Tingting 合成参考，不是女儿声音，也没有录制环境声音。第一次测试用大整数数组传递参考音频时窗口退出；改为 base64 传送测试夹具后完整通过，产品录音路径不使用这种测试数组。
- 独立真实推理 `node scripts/voice-ai-smoke.cjs`：约 36.178 秒生成 3.36 秒“耶，击掌！这件事完成啦。”；记录和音频为 `artifacts/0.7.0-AI链路测试.json`、`0.7.0-AI链路测试-非女儿声音.wav`。验证的是本机生成和可解码输出，不声称人工听辨/儿童声线相似度通过。
- 最终包还通过 `scripts/smoke.cjs`、`scripts/companionship-smoke.cjs`、`scripts/wardrobe-smoke.cjs`。本轮截图单独保存到 `artifacts/0.7.0-regression/`，不覆盖历史版本证据。包含设置/100条导入/重启、安静按钮隐藏、两阶段击掌/收回/超时、饭点/预览隔离、衣橱试穿/应用/重启/恢复/窄屏。
- 实际配音室截图：`artifacts/0.7.0-小小配音室.png`、`0.7.0-配音室窄窗口.png`，已打开检查；两栏宽屏与单栏窄屏无横向溢出，状态栏调整后不遮挡配音操作。`0.7.0-AI测试状态-非女儿声音.png` 仅是合成样本的集成测试证据。
- 最终包 src 与工作区 src 逐文件一致，校验记录 `artifacts/0.7.0-source-manifest.json`；源码快照 `artifacts/cike-0.7.0-source.tar.gz`，不包含用户录音、模型、Python 环境、依赖或原始参考照片。
- 限制：未录制女儿声音、未听辨相似度；真实物理麦克风授权和录音待家长操作。未重新跑全部 motion/visual/lab 历史套件；未做长期调度、其他 Mac、Intel 或 Windows。本机 AI 当前只支持 Apple Silicon。生成可有吞字或重复，必须逐句试听启用；仍是未签名公证的本机测试应用，无提交、推送或发布。

---

# wardrobe-20260922 · 小衣橱与皮肤（2026-09-22）

- 基线：当前工作区 package 0.7.0（同期配音开发存在）；本轮修改 appearance/laboratory、动作及击掌手部颜色，新增衣橱测试，保留其他开发工作。
- `npm test`：42/42 通过。新增旧四字段外观迁移、颜色/枚举输入校验、皮肤衣服持久化及全场景/动作/击掌外观保留。原先默认外观的顺序比较问题已修复，默认场景仍保持旧图。
- `node scripts/wardrobe-smoke.cjs`：开发态通过；指定 `CIKE_EXECUTABLE` 为 `dist/wardrobe-20260922/mac-arm64/此刻.app/Contents/MacOS/此刻` 的最终应用包再次通过。
- 真实 UI 验证：五种穿着选项切换，换装/换造型保留皮肤，未应用不改持久化外观，应用/重启/恢复；60 组造型×衣服×花纹及35动作 SVG 实际解码；760px 窗口无横向溢出；穿衣完成行动→举手→击掌→按钮隐藏→收回。
- 已检查实际截图：`../artifacts/wardrobe-20260922/小衣橱-实际界面.png`、`小衣橱-窄窗口.png`、`小衣橱-桌面.png`、`小衣橱-击掌.png`。最终截图来自打包应用；界面截图已直接展示在聊天。
- 本轮未逐帧人工审查全部35动作，不宣称已完成最终美术验收；无 AI 生图接入、无最终 IP 定稿，不替代配音能力专属验收。旧 smoke/motion 全量 UI 套件未重跑，不混作本轮证据。
- Mac arm64 本地测试包；无签名公证、Windows验收、提交、推送或发布。所有 UI 测试使用独立临时档案并清理，未修改用户真实偏好。

---

## 2026-09-22 · 皮肤与场景生产需求记录

用户后续回复“确认”，小衣橱方案及首批四种衣服方向已确认；具体视觉、IP 母版及生成结果仍未验收。本次仅同步方案状态，未改应用、未运行产品测试。

后续衣服选择反馈已追加至产品判断记录，并同步 README。衣橱与服装资产尚未实现；仅文档更新，未运行应用或新增视觉验收。下轮需检查换装保持皮肤与五官、桌面尺寸可辨及动作中衣袖接触关系。

仅更新产品判断与 README 当前继续点。已核对 package.json 为 0.6.1、应用目录存在、src/appearance.js 现有外观字段；皮肤配置与 AI 批量场景生成尚未实现。未启动应用、未重新运行测试或生成截图；以下为既有版本验证证据。下一步按记录中的外观配置和母版确认流程推进，具体方案待评审。

# 0.6.1 · 收起击掌入口（2026-09-22）

- 用户纠正已落实：安静时隐藏按钮；行动卡进入举手等待后才显示；击中立即隐藏按钮，完成表情约 2.6 秒后收起；未回应 15 秒自动收起。
- 36/36 单测通过；最终 0.6.1 应用包 companionship 回归通过，新增首次打开、击中、收回、真实 15 秒等待超时后的不可见断言。页面错误为零。
- 已查看实际截图 `artifacts/0.6.1-安静表情.png`，确认无常驻手掌按钮。同版本举手、完成截图也已保存。
- 应用：`dist/0.6.1/mac-arm64/此刻.app`。恢复快照与待评审状态见版本索引。未提交、推送、发布；其他发行与实测边界沿用 0.6.0。

---

# 0.6.0 · 击掌与安静陪伴（2026-09-22）

- 基于 0.5.0，保留既有矢量角色和双材质试映；真实实现两阶段击掌、安静静态轮换、饭点过滤、模拟预览标签与过时普通卡收起。
- `npm test`：36/36。新增旧库午饭在下午/傍晚/夜间不可出现，以及两阶段手势参数和静态输出检查。
- `test:smoke`、`test:visual`、`test:lab`、`test:motion` 全部通过，覆盖原有设置/导入/持久化、卡片与自定义图、拖动/去重、一次动作和减少动画。
- `node scripts/companionship-smoke.cjs`：真实 Electron 中点击完成→举手→点手掌→完成→收回，轮换姿态、减少动画阻止轮换、时段预览与正常取卡区分、过时卡收起、零页面错误。新 0.6.0 打包应用同样通过。
- 证据：`artifacts/0.6.0-举手击掌.png`、`0.6.0-击掌完成.png`、`0.6.0-安静表情.png`；已打开实际截图检查手掌命中区与角色位置。
- 静态变化计时在 UI 测试中直接触发同一轮换函数；未完成多小时陪伴、跨午夜休眠或多显示器实机验收。饭点语义保护覆盖早餐/早饭、午餐/午饭、晚餐/晚饭文字，不声称识别任意导入文案的所有时间语义。
- 击掌使用鼠标/触控板或键盘按钮，不是摄像头手势识别。用户上传图仍保持原图，通过按钮与文字反馈。没有完成次数、打卡或成绩持久化。
- 0.6.0 Mac arm64 本地应用位于 `dist/0.6.0/mac-arm64/此刻.app`，未签名公证；Windows 未验证。仓库无提交，已保存源码快照，见版本索引。无推送或发布；视觉和体验待用户评审。

---

## 2026-09-22 项目记忆接入

仅整理 AGENTS、README、产品判断、参考入口和历史状态标识；未修改产品代码。核对 package.json 为 0.5.0、Mac 应用目录与独立试映入口存在。下文测试为此前执行的结果，本轮没有重新运行产品测试。用户“可以试试”仅表示探索许可，技术测试通过不等于最终视觉认可。研究子 agent 的新增结果须经核实后补记。

---

## 2026-09-22 双材质独立试映页

`node scripts/trial-smoke.cjs` 已通过：两图 1254px 解码；3 动作 × 3 帧切换；单次播放完成后像素稳定；换动作取消旧计时器；应用及系统减少动画；定时触发与取消；390px 窄屏无横向溢出；页面无错误。隔离测试数据目录已清理，未修改用户应用档案。

视觉截图：`artifacts/双材质试映截图.png`、`artifacts/双材质试映窄屏.png`。本轮没有重新打包 Electron，0.5.0 安装包保持原样。此轮阅读范围与图像质量限制见收藏逐篇阅读记录。

# 0.5.0 · 具体动作与一次播放（2026-09-22）

- 已交付两张 imagegen 原创质感方向图、灵感与动作工作台、35 种具体动作、120 条行动文案逐条 motionId 映射；未替换用户的默认形象。
- 自动/主动提醒出现时播放 3.2 秒一次，完成后静止；新提醒取消旧动作；静态常驻，不无限呼吸/跳动。应用和系统减少动画均可停用。
- `npm test`：34 项通过。覆盖白名单、动作/场景配对、旧库兼容、动作持久化与 35×2×3 种动作场景输出。
- `test:smoke`、`test:visual`、`test:lab` 回归均通过。
- `test:motion`：实际像素中间态与终态不同，终态两次截图相同；旧动作取消、减少动画、35 动作解码和逐个播放、筛选及窄窗口通过。终版打包应用亦运行该验收。
- 交付 `dist/0.5.0/mac-arm64/此刻.app` 及对应本地测试 ZIP。仍未作 Developer ID 签名/公证或 Windows 实机测试。
- 范围限制：高质感 A/B 是方向图，不是已制作的成套动画；35 动作采用既有矢量角色验证机制。小红书仅确认收藏页、首屏封面和首批 30 标题；控制连接失败，零篇详情读完，完整审美研究未完成。已明确告知用户，待浏览器重新连接。

---

# 0.4.0 · 常驻场景与可爱实验室（2026-09-22）

- 已实现：默认七时段常驻生活场景；点击/回车取卡；6 px 拖动阈值；手动每轮去重且不改变自动提醒记忆；实验室三种造型、单变量 A/B、实际尺寸场景、昼夜预览、应用和恢复。
- `npm test`：30 项通过。旧档案兼容、非法参数拒绝、所有造型 × 八场景 × 昼夜生成检查通过。
- Electron UI：设置保存、七时段预览、导入成功/失败、历史保留、卡片最长文案、自动收起/停留、自定义图、缺图回退、窄窗口均覆盖。
- 新 UI 验收：连续六次点击无重复，自动额度不变；拖动不出卡；回车可用；外观设置保存后重启有效；原形象可恢复；760 px 实验室无横向溢出。
- 已检查实际截图：`artifacts/常驻场景.png`、`artifacts/点击行动卡.png`、`artifacts/可爱实验室.png`、`artifacts/可爱实验室-窄窗口.png`。截图审阅发现的嘴巴路径错误已修复并重新截图。
- 输出：`dist/0.4.0/mac-arm64/此刻.app`；`dist/此刻-0.4.0-mac-arm64-本地测试.zip`。旧版本保留。
- 范围：本机 Apple Silicon Mac 测试版；未做 Developer ID 签名、公证或 Windows 实机测试。造型是原创矢量插图，不宣称达到参考图的手绘质感或已经证明促进实际行动。

---

# 此刻 0.3.0 验收记录

日期：2026-09-22。

- `npm test`：**25/25 通过**。新增行动卡比例、元数据校验、非法图片路径拒绝、标题与边界提示的禁忌过滤、行动字段持久化与旧词库兼容。
- 最终 `dist/0.3.0/mac-arm64/此刻.app` 通过真实 Electron 冒烟测试：七时段预览、设置保存、100条导入、错误导入保留旧库、重启恢复。
- 最终包通过视觉/交互测试：8组昼夜SVG实际解码，停留超过30秒不消失、恢复自动收起、主动收起、长文案不裁切、540px窄窗口无横向溢出、自定义图片不被替换、图片失败回退默认形象。
- 已查看真实截图：`artifacts/行动卡-喝水.png`、`artifacts/行动卡-桌角.png`、`artifacts/行动卡-明天一行字.png`、`artifacts/行动预览-窄窗口.png`。桌面截图直接截取窗口可见内容区域，未拼接或生成模拟截图；展示用例通过实际IPC渲染本地真实词库条目。时钟加速只用于测试自动收起。
- 136条内容中，120条具备具体动作/大致用时/结束边界，16条保留纯陪伴。没有完成任务追踪。
- 原版本保留，新版ZIP：`dist/此刻-0.3.0-mac-arm64-本地测试.zip`。仍为Mac arm64未签名本地测试版；签名公证、Windows实机与真实行动转化效果不在本次已验证范围。

以下保留历史验收记录。

---

# 此刻 0.2.0 验收记录

日期：2026-09-22。沿用 macOS arm64 / Electron 41.10.7 的验证环境。

- 核心测试 **21/21 通过**，新增分钟边界、跨午夜作息、个人起床和收尾时段、全天分散机会、跨午夜机会不重复、七场景匹配、周末/周一过滤、偏好加权、近期主题降权、语气文本禁忌过滤、预览零记忆写入、旧档案与旧词库兼容。
- 开发态和最终 **0.2.0 打包应用**均通过真实 Electron 测试：七时段逐项选择与试听、预览前后记忆完全一致、设置保存、100 条导入、错误导入不覆盖、重启恢复、渲染器没有 Node require。
- 新版真实界面截图：artifacts/时段陪伴-设置.png、artifacts/时段陪伴-试听卡片.png、artifacts/时段陪伴-午间.png。已查看确认卡片排版和气泡展示。
- 新版本单独保存在 dist/0.2.0/mac-arm64/此刻.app，旧版保留。归档为 dist/此刻-0.2.0-mac-arm64-本地测试.zip。
- 仍是未签名、未公证的本地测试版。Windows、多屏和真实睡眠事件的人工实机验收边界不变。
- 新版包含 80 条原始 + 56 条场景内容；场景内容还有两种额外语气。当前使用离线规则选取，未新增联网模型调用。

以下保留 0.1.0 的原始验收记录。

---

# 此刻 0.1.0 验收记录

日期：2026-09-21。环境：macOS 26.5.2 / Apple Silicon arm64，Node.js 24.14.1，Electron 41.10.7，electron-builder 26.15.3，Playwright 1.63.0。

## 已通过

- `npm test`：9 / 9。包括 80 条唯一性、跨午夜静默、同小时静默关闭、间隔、每日上限、次日重置、闲置、停用、最近去重、屏蔽、时段筛选、候选耗尽、非法库拒绝、设置校验、磁盘持久化、损坏备份和禁忌词过滤。
- `npm run test:smoke`：真实 Electron 窗口通过。检查渲染进程无 require；显示气泡；操作设置表单；跨窗口状态同步；生成带偏好的提示词；导入 100 条；错误导入不覆盖旧库；拒绝越界设置；关闭进程并重启恢复昵称与 100 条专属内容；未捕获页面错误为 0。
- `npm run pack -- --config.electronDist=node_modules/electron/dist`：最终 macOS arm64 应用包生成成功，使用原创应用图标。
- 指定 `CIKE_EXECUTABLE` 为 dist/mac-arm64/此刻.app/Contents/MacOS/此刻，再跑相同 smoke：通过。此项验证了实际打包产物，不只是源码运行。
- 真实窗口截图：artifacts/桌面小生命.png、artifacts/偏好设置.png，已查看确认形象、文字、设置和按钮无裁切。气泡由测试明确触发，用于验证渲染；不是声称深夜调度自动发出了下午内容。
- ZIP 使用 macOS ditto 打包，包含当前最终 .app。

## 测试方法边界

测试使用独立临时 userData，结束清理，不污染真实用户记忆。导入测试以固定文件替代原生选择器的返回值；文件解析、校验、存储和 UI 更新使用真实应用逻辑。气泡测试用真实 IPC 渲染路径注入固定句子。原生右键菜单、拖动、换图、长时间调度、睡眠锁屏事件、多个显示器和第三方软件全屏叠放尚未逐项做人工实机回归；其核心筛选规则已有单元测试，但不把它们冒充完整系统验收。

## 发行边界

这是本地测试版：未使用 Developer ID 签名，未 Apple 公证，未完成其他 Mac / Intel Mac / Windows 实机兼容验收。没有生成 Windows 安装包，也没有宣称通过 Windows 验收。没有实际开户、创建爱发电方案、发送消息、发布销售页或收取付款。

付费凭证只是手动售后标识，当前客户端无激活墙；首发商业回馈是一次专属短句制作服务。AI 目前采用复制提示词到用户选定的模型再导入，没有后台自动生成接口。

## 下载故障处理记录

默认 GitHub Electron 二进制下载在当前网络超时。使用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/` 完成下载，安装器按 Electron npm 包自带 checksums.json 校验；未关闭证书校验或校验和验证。最终包从已验证的本地 Electron distribution 构建。

若同环境重新安装时默认下载超时：

```bash
ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ npm install
```

正常网络可直接 npm install / npm ci。镜像不是产品运行依赖，应用日常不访问镜像。

## ip-trial-20260922-01 · 收藏方向三动作试样
2026-09-22：实际生成喝水、合本子、调灯三张，固定0.8.1独立临时档案调用保存与预览，三项图像内容与动作ID断言通过。截图见artifacts/ip-trial-20260922-01/*-app.png。已视觉检查调灯截图：浅底矩形明显、主体偏小、植物细节偏多；仅画风试验，非透明桌宠完成品。未改用户默认档案，未修改应用源码/包版本，未提交/推送/公开发布。提示词、对应关系和校验在同目录。豆包/WorkBuddy未测，收藏本轮未连通。
# 0.9.0 Seedream 形象生成接入 · 2026-09-23

- 已实现：`src/seedream.cjs` 复用 awafun 的火山方舟 Seedream 接口约定，支持照片生成桌面 IP 形象、以已应用形象生成小动作配图；结果只在用户确认后应用，本地保存；无密钥时明确报错；API 密钥不进入页面和配置备份。
- 已验证：Seedream prompt 单测、动作编辑器既有定制单测、JS 语法检查、`git diff --check`。
- 未验证：真实 Seedream API 请求、真实费用、儿童照片合规流程、代理部署、6 张候选的真实视觉一致性、最终打包应用。
- 边界：当前默认直接支持 `ARK_API_KEY` 开发测试；公开分发应使用受控的 `CIKE_IMAGE_API_URL` 服务端代理、用户身份/额度和费用策略，不能把 ARK 密钥硬编码到应用。
- Agent Finder 结果仅作为提示词/图片生成辅助参考，未自动安装第三方 skill；运行时能力由本地 provider 实现。
- 下一步：配置安全代理并用获得授权的测试照片做一次真实生成验收，再评估 Seedream 版本、额度和候选数量。
